# ============================================
# BC Agency PMS - Production Environment Variables
# ============================================
# IMPORTANT: Copy this file to .env.production and fill in real values
# NEVER commit .env.production to version control
# ============================================

# ============================================
# APPLICATION
# ============================================
NODE_ENV=production
APP_NAME=bc-agency-pms

# ============================================
# DOMAIN & SSL
# ============================================
DOMAIN=pms.bcagency.vn
SSL_EMAIL=admin@bcagency.vn

# ============================================
# DATABASE - PostgreSQL
# ============================================
# Use strong, randomly generated passwords in production
# Generate with: openssl rand -base64 32
POSTGRES_USER=bc_pms_user
POSTGRES_PASSWORD=CHANGE_THIS_STRONG_PASSWORD_32_CHARS
POSTGRES_DB=bc_pms_production

# ============================================
# CACHE - Redis
# ============================================
# Generate with: openssl rand -base64 32
REDIS_PASSWORD=CHANGE_THIS_REDIS_PASSWORD_32_CHARS

# ============================================
# FILE STORAGE - MinIO
# ============================================
# Generate strong credentials for MinIO
# Access key: minimum 3 characters
# Secret key: minimum 8 characters (recommended 32+)
MINIO_ACCESS_KEY=bcpmsadmin
MINIO_SECRET_KEY=CHANGE_THIS_MINIO_SECRET_KEY_32_CHARS
MINIO_BUCKET=bc-agency-files

# ============================================
# AUTHENTICATION - JWT
# ============================================
# Generate with: openssl rand -base64 64
# CRITICAL: Use a strong, unique secret in production
JWT_SECRET=CHANGE_THIS_JWT_SECRET_MINIMUM_64_CHARACTERS_LONG_FOR_SECURITY
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# ============================================
# ENCRYPTION
# ============================================
# Generate with: openssl rand -base64 32
ENCRYPTION_SECRET=CHANGE_THIS_ENCRYPTION_SECRET_32_CHARS
ENCRYPTION_SALT=CHANGE_THIS_SALT_16

# ============================================
# TELEGRAM NOTIFICATIONS (Optional)
# ============================================
# Get bot token from @BotFather on Telegram
# Get chat ID from @userinfobot or @getidsbot
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_ALERT_CHAT_ID=your-telegram-chat-id

# ============================================
# RATE LIMITING
# ============================================
# Requests per TTL window
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100

# ============================================
# MONITORING (Optional)
# ============================================
# Grafana admin password
GRAFANA_PASSWORD=CHANGE_THIS_GRAFANA_PASSWORD
# Prometheus data retention period
PROMETHEUS_RETENTION=30d

# ============================================
# BACKUP CONFIGURATION
# ============================================
# Cron schedule for automated backups (optional)
BACKUP_SCHEDULE=0 2 * * *
# Number of backups to retain
BACKUP_RETENTION_DAYS=30

# ============================================
# LOGGING
# ============================================
# Log level: debug, info, warn, error
LOG_LEVEL=info

# ============================================
# SECURITY NOTES
# ============================================
# 1. Generate all secrets using: openssl rand -base64 32 (or 64 for JWT)
# 2. Never share or commit this file with real values
# 3. Rotate secrets periodically (recommended: every 90 days)
# 4. Use different secrets for different environments
# 5. Store secrets securely (consider using a secrets manager)
#
# Commands to generate secrets:
# - JWT_SECRET: openssl rand -base64 64
# - Other secrets: openssl rand -base64 32
# - Short passwords: openssl rand -base64 16
